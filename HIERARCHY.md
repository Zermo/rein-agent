# Product line

Three layers. Same house. Not sibling names.

1. **rein-agent** (`main`) — CLI harness. REPL, loop, tools, any OpenAI-compatible model.
2. **reinklaud** (`reinklaud`) — klaʊdbot field console + iOS WKWebView of the live site. Secondary line off the harness.
3. **dareecho** (`dareecho`) — OS tier. Final form.

`ios` remains the TestFlight-only lane (manual okay). Do not treat it as the product branch.
