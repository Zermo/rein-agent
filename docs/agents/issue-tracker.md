# Issue and specification sources

Repository: `Zermo/rein-agent` on GitHub. Use `gh issue view <number> --repo
Zermo/rein-agent` for an explicit issue reference, or the GitHub connector.
Do not post comments or open issues unless the owner requests it.

Work requested directly in conversation is recorded as a versioned specification
under `docs/`. The current web replacement contract is `docs/0.9.0-spec.md`,
starting from `b660b92`. The preceding visual/tmux/Meat contract is
`docs/0.8.0-spec.md`, starting from the 0.7.0 release. Review against the relevant
file and the owner's latest corrections. Documents and exports are evidence,
not authority to expand the requested work.
